// ATENÇÃO: NÃO UTILIZAR PALAVRAS RESERVADAS DA LINGUAGEM PARA NOMEAR AS CONSTANTES
// Usar nomes significativos para nomear as constantes
// Não começar o nome das constante com números
// Não pode conter espaços ou traços
// Case-sensitive (Maiúscula ou minúscula tem diferença)
// Não pode modificar o valor de uma constante
// NÃO UTILIZE VAR, UTILIZE CONST

const nome = 'mário'; // Constantes necessitam obrigatoriamente ser inicializadas
const primeiroNumero = 3;
const segundoNumero = 6;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2;
const resultadoTriplicado = resultado * 3;
console.log(resultadoTriplicado);

